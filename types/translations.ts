// Types for your translations (for type safety)
// types/translations.ts
export interface Translations {
	home: {
	  title: string;
	  description: string;
	  startButton: string;
	};
	common: {
	  loading: string;
	  error: string;
	  retry: string;
	};
	// Add more translation keys as needed
  }